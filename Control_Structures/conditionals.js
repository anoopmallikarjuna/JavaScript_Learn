const arr = []

if(arr.length === 0) {
    console.log("Array is empty")
}

const Obj = {}

if(Object.keys(Obj).length === 0) {
    console.log("Object is empty")
}

const obj1 = null ?? 10

console.log(obj1);
